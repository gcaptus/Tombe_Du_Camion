
{% block body %}

    <nav class="navbar-global">  
        <div class="row col-6">
            <div class="collapse navbar-collapse col" id="navbarSupportedContent" >
                  <a class="navbar-brand" href="/"><img src="{{ asset('asset/images/logo.png') }}" alt=""></a>
                 {{ form_start(form, { 'attr': {'action': path('results')} }) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
                </div>